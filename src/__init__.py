"""Shared code for the thesis pipeline (01_data -> 02_sentiment -> 03_portfolio -> 04_results).

Install once from the repo root with `pip install -e .`, then import from any
notebook as e.g. `from src.config import PROJECT_ROOT`.
"""
